<!DOCTYPE html>
<html>
  <%= render :partial => "/admin/head" %>
  <body<% " class='login #{::Refinery::I18n.current_locale}'" unless user_signed_in? %><% " class='splash #{::Refinery::I18n.current_locale}'" if user_signed_in? and action_name == "index" %>>
    <%= render :partial => '/shared/site_bar'  %>
    <div id='tooltip_container'></div>
    <div id='header'>
      <div id='header_content'>
        <% if user_signed_in? %>
          <%= render :partial => "/admin/menu" %>
        <% else %>
          <h1><%= yield :header %></h1>
        <% end %>
      </div>
    </div>
    <div id="page_container">
      <div id="page">
        <div id="content" class="clearfix">
          <div id="flash_container">
            <%= render :partial => "/shared/message" %>
          </div>
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
